/// @description Setup spawning and timers
// Commenting out since we no longer have an ocean - 4/20/2022

/*

// Set a random spawn interval between 30s - 60s
spawnInterval = random_range(room_speed*30, room_speed*60);

// Set first item to spawn 
obj_item = obj_controller.random_item();

// Set spawn alarm
alarm[0] = spawnInterval;

*/
